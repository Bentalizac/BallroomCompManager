# Supabase Configuration
# Get these from your Supabase project dashboard
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Server Configuration
PORT=3001
NODE_ENV=development

# Notes:
# - SUPABASE_URL: Your Supabase project URL (local: http://localhost:54321)
# - SUPABASE_ANON_KEY: Public anon key (safe to expose to clients)
# - SUPABASE_SERVICE_ROLE_KEY: Secret service role key (server-side only, bypasses RLS)
# - Get local keys by running 'supabase start' and copying from output
# - For production, get keys from https://app.supabase.com/project/YOUR_PROJECT/settings/api
